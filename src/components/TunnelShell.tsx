"use client";

// Temporarily disable the tunnel shell (walls/ceiling) to allow debugging
// and tuning of camera, cart and other scene elements without occlusion.
const TunnelShell = () => null;

export default TunnelShell;
