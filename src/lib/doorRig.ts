/**
 * Igloo entrance door — tune scale / placement here.
 */
export const DOOR_RIG = {
  scale: 4,
  /** Meters before track end (along approach), door faces the cart. */
  approachOffset: 1.1,
  /** Height above the rail bed at the end of the branch. */
  height: 2.5,
};
